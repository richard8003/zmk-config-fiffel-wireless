#include "keypos.dtsi"

// please
combo_please {
  timeout-ms = <50>;
  key-positions = <POS_P POS_L>;
  bindings = <&kp LC(LG(LA(LS(A))))>;
};

// academy
combo_academy {
  timeout-ms = <50>;
  key-positions = <POS_A POS_Y>;
  bindings = <&kp Z>;
};
